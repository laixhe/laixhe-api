definitions:
  core.Error:
    properties:
      code:
        type: integer
      message:
        type: string
    required:
    - code
    - message
    type: object
  core.HealthResponse:
    properties:
      database:
        description: "数据库状态: up / down"
        type: string
      now:
        description: 当前时间 (RFC3339, UTC)
        type: string
      started_at:
        description: 服务启动时间 (RFC3339, UTC)
        type: string
      status:
        description: "服务状态: ok / degraded"
        type: string
      version:
        description: 服务版本
        type: string
    required:
    - status
    - database
    - version
    - started_at
    - now
    type: object
  entity.AuthLoginRequest:
    properties:
      email:
        description: 邮箱
        type: string
      password:
        description: 密码
        type: string
    required:
    - email
    - password
    type: object
  entity.AuthLoginResponse:
    properties:
      token:
        description: jwt token
        type: string
      user:
        allOf:
        - $ref: '#/definitions/entity.User'
        description: 用户信息
    required:
    - token
    - user
    type: object
  entity.AuthRefreshResponse:
    properties:
      token:
        description: jwt token
        type: string
      user:
        allOf:
        - $ref: '#/definitions/entity.User'
        description: 用户信息
    required:
    - token
    - user
    type: object
  entity.AuthRegisterRequest:
    properties:
      email:
        description: 邮箱
        type: string
      nickname:
        description: 昵称
        type: string
      password:
        description: 密码
        type: string
    required:
    - email
    - nickname
    - password
    type: object
  entity.AuthRegisterResponse:
    properties:
      token:
        description: jwt token
        type: string
      user:
        allOf:
        - $ref: '#/definitions/entity.User'
        description: 用户信息
    required:
    - token
    - user
    type: object
  entity.User:
    properties:
      account:
        description: 账号
        type: string
      avatar_url:
        description: 头像地址
        type: string
      created_at:
        description: 创建时间
        type: string
      email:
        description: 邮箱
        type: string
      mobile:
        description: 手机号
        type: string
      nickname:
        description: 昵称
        type: string
      sex:
        allOf:
        - $ref: '#/definitions/models.UserSex'
        description: |-
          UserSex:
          * 0 - 未填写
          * 1 - 男
          * 2 - 女
      states:
        allOf:
        - $ref: '#/definitions/models.UserState'
        description: |-
          UserState:
          * 0 - 禁用
          * 1 - 正常
      type_id:
        allOf:
        - $ref: '#/definitions/models.UserType'
        description: |-
          UserType:
          * 1 - 普通用户
      uid:
        description: 用户id
        type: integer
    required:
    - account
    - avatar_url
    - created_at
    - email
    - mobile
    - nickname
    - sex
    - states
    - type_id
    - uid
    type: object
  entity.UserListResponse:
    properties:
      list:
        description: 列表
        items:
          $ref: '#/definitions/entity.User'
        type: array
      page:
        description: 分页-当前页
        type: integer
      page_size:
        description: 分页-每页数量
        type: integer
      total:
        description: 总数
        type: integer
    required:
    - list
    - page
    - page_size
    - total
    type: object
  entity.UserUpdateRequest:
    properties:
      avatar_url:
        description: 头像地址
        type: string
      nickname:
        description: 昵称
        type: string
    required:
    - avatar_url
    - nickname
    type: object
  models.UserSex:
    enum:
    - 0
    - 1
    - 2
    type: integer
    x-enum-comments:
      UserSexFemale: 女
      UserSexMale: 男
      UserSexUnknown: 未填写
    x-enum-descriptions:
    - 未填写
    - 男
    - 女
    x-enum-varnames:
    - UserSexUnknown
    - UserSexMale
    - UserSexFemale
  models.UserState:
    enum:
    - 0
    - 1
    type: integer
    x-enum-comments:
      UserStateBanned: 禁用
      UserStateNormal: 正常
    x-enum-descriptions:
    - 禁用
    - 正常
    x-enum-varnames:
    - UserStateBanned
    - UserStateNormal
  models.UserType:
    enum:
    - 1
    type: integer
    x-enum-comments:
      UserTypeOrdinary: 普通用户
    x-enum-descriptions:
    - 普通用户
    x-enum-varnames:
    - UserTypeOrdinary
info:
  contact: {}
  description: 用户认证与用户管理 API 服务
  title: API接口
  version: "1.0"
paths:
  /api/v1/swagger:
    get:
      description: 浏览器直接访问该地址即可查看与调试全部接口
      produces:
      - text/html
      responses:
        "200":
          description: Swagger UI HTML 页面
      summary: Swagger UI 接口文档页面
      tags:
      - System
  /api/v1/auth/login:
    post:
      consumes:
      - application/json
      parameters:
      - description: 请求参数
        in: body
        name: req
        required: true
        schema:
          $ref: '#/definitions/entity.AuthLoginRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK (结构见 entity.AuthLoginResponse)
          schema:
            $ref: '#/definitions/entity.AuthLoginResponse'
        "422":
          description: Unprocessable Entity
          schema:
            $ref: '#/definitions/core.Error'
        "429":
          description: 请求过于频繁，限流触发
          schema:
            $ref: '#/definitions/core.Error'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/core.Error'
      summary: 登录
      tags:
      - Auth
  /api/v1/auth/refresh:
    post:
      consumes:
      - application/json
      parameters:
      - description: Bearer XXX令牌
        in: header
        name: Authorization
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK (结构见 entity.AuthRefreshResponse)
          schema:
            $ref: '#/definitions/entity.AuthRefreshResponse'
        "401":
          description: Unauthorized
          schema:
            $ref: '#/definitions/core.Error'
        "429":
          description: 请求过于频繁，限流触发
          schema:
            $ref: '#/definitions/core.Error'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/core.Error'
      summary: 刷新Jwt
      tags:
      - Auth
  /api/v1/auth/register:
    post:
      consumes:
      - application/json
      parameters:
      - description: 请求参数
        in: body
        name: req
        required: true
        schema:
          $ref: '#/definitions/entity.AuthRegisterRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK (结构见 entity.AuthRegisterResponse)
          schema:
            $ref: '#/definitions/entity.AuthRegisterResponse'
        "422":
          description: Unprocessable Entity
          schema:
            $ref: '#/definitions/core.Error'
        "429":
          description: 请求过于频繁，限流触发
          schema:
            $ref: '#/definitions/core.Error'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/core.Error'
      summary: 注册
      tags:
      - Auth
  /api/v1/health:
    get:
      consumes:
      - application/json
      description: 探测服务与数据库是否正常，可用于负载均衡/容器编排探活
      produces:
      - application/json
      responses:
        "200":
          description: OK (结构见 core.HealthResponse)
          schema:
            $ref: '#/definitions/core.HealthResponse'
        "503":
          description: 数据库不可用 (code=503)
          schema:
            $ref: '#/definitions/core.Error'
      summary: 健康检查
      tags:
      - System
  /api/v1/user/info:
    get:
      consumes:
      - application/json
      parameters:
      - description: 用户id
        in: query
        name: uid
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: OK (结构见 entity.User)
          schema:
            $ref: '#/definitions/entity.User'
        "422":
          description: Unprocessable Entity
          schema:
            $ref: '#/definitions/core.Error'
        "429":
          description: 请求过于频繁，限流触发
          schema:
            $ref: '#/definitions/core.Error'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/core.Error'
      summary: 获取用户信息
      tags:
      - User
  /api/v1/user/list:
    get:
      consumes:
      - application/json
      parameters:
      - description: 分页-当前页(默认 1)
        in: query
        name: page
        required: true
        type: integer
      - description: 分页-每页数量(默认 12)
        in: query
        name: page_size
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: OK (结构见 entity.UserListResponse)
          schema:
            $ref: '#/definitions/entity.UserListResponse'
        "422":
          description: Unprocessable Entity
          schema:
            $ref: '#/definitions/core.Error'
        "429":
          description: 请求过于频繁，限流触发
          schema:
            $ref: '#/definitions/core.Error'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/core.Error'
      summary: 获取用户列表
      tags:
      - User
  /api/v1/user/update:
    post:
      consumes:
      - application/json
      parameters:
      - description: Bearer XXX令牌
        in: header
        name: Authorization
        required: true
        type: string
      - description: 请求参数（Uid 由 JWT 提供）
        in: body
        name: req
        required: true
        schema:
          $ref: '#/definitions/entity.UserUpdateRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK (结构见 entity.User)
          schema:
            $ref: '#/definitions/entity.User'
        "422":
          description: Unprocessable Entity
          schema:
            $ref: '#/definitions/core.Error'
        "401":
          description: Unauthorized
          schema:
            $ref: '#/definitions/core.Error'
        "429":
          description: 请求过于频繁，限流触发
          schema:
            $ref: '#/definitions/core.Error'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/core.Error'
      summary: 更新用户信息
      tags:
      - User
swagger: "2.0"
