syntax = "proto3";

package pbuser;

option go_package = "webapi/api/gen/pbuser;pbuser";

message User {
  // 用户ID
  uint64 uid  = 1;
  // 用户名
  string uname = 2;
  // 用户邮箱
  string email = 3;
  // 创建时间
  string created_at = 4;
}
